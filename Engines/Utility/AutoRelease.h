#pragma once

namespace CR
{
	namespace Utility
	{
		/*template<class T>
		class AutoRelease : public T
		{
		public:

			AutoRelease(T *_ptr) :ptr(_ptr)
			{
			}

			virtual ~AutoRelease(void)
			{
				ptr->Release();
			}
		private:
			T *ptr;
		};*/
	}
}
