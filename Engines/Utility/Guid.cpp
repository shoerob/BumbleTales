//#include "StdAfx.h"
#include "Guid.h"

using namespace CR::Utility;
/*
std::size_t CR::Utility::hash_value(const CR::Utility::Guid &_guid)
{
	std::size_t seed = 0;
	boost::hash_combine(seed, _guid.Data1());
	boost::hash_combine(seed, _guid.Data2());
	boost::hash_combine(seed, _guid.Data3());
	boost::hash_combine(seed, _guid.Data4());

	return seed;
}*/